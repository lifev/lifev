# -*- getpot -*- (GetPot mode activation for emacs)
#-------------------------------------------------
#      Data file for Laplacian Solver
#-------------------------------------------------


[adr]

[./physics]

diffusivity     = 1.0           # diffusivity


[../discretization]

order     = P1
mesh_dir  = ./Mesh/  # the directory where the mesh file is
#mesh_file = hexa32x32.mesh        # mesh file
mesh_file = hexa16x16.mesh        # mesh file
#mesh_file = hexa8x8.mesh        # mesh file
#mesh_file = hexa4x4.mesh        # mesh file
#mesh_file = cube8x8.mesh        # mesh file
# mesh_file = cube16x16.mesh      # mesh file
#mesh_file = cube32x32.mesh      # mesh file

[../prec]
prectype                = ML
reuse                   = false

[./ML]
default_parameter_list = SA    # for ML precond, SA, DD, DD-ML, maxwell, NSSA, DD-ML-LU, DD-LU
displayList             = true
analyze_smoother	= false

[./smoother]

type                    = IFPACK
pre_or_post             = both
sweeps                  = 3
damping_factor          = 1

[../coarse]

type                    = Gauss-Seidel
sweeps                  = 1
pre_or_post             = both
#max_size                = 64

[../repartition]
enable                  = 0
partitioner             = ParMETIS
max_min_ration          = 1.3
min_per_proc            = 500


# Parameters for the Ifpack preconditioner

[../]
[../ifpack]
Prectype               = Amesos    # Amesos (does a local LU factorization), ILUT (not ILU)

[./relaxation]
type                   = Jacobi
damping_factor         = 1.
sweeps                 = 1
min_diagonal_value     = 0
zero_starting_solution = true

[../partitioner]
type                   = parmetis
overlap                = 0
local_parts            = 1
root_node              = 0
use_symmetric_graph    = true

[../amesos]
solvertype             = Amesos_KLU

[../fact]
drop_tolerance         = 1.e-5
level-of-fill          = 1
ilut_level-of-fill     = 1
absolute_threshold     = 0.
relative_threshold     = 0.
relax_value            = 0.

[../schwarz]
combine_mode           = true
reordering_type        = none
filter_singletons      = false

[../]
[../]
[../solver]
solver          = gmres
scaling         = none
output          = none  # all_res none
conv            = rhs
max_iter        = 100
kspace          = 100
tol             = 1.e-10    # AztecOO tolerance


[exporter]
multimesh  =    false   # actually we export also the displacement
start      = 0
save       = 1
