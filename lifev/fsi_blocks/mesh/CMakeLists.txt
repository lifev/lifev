SET(mesh_HEADERS
  mesh/MeshPartitionerOfflineFSI.hpp
CACHE INTERNAL "")

SET(mesh_SOURCES
CACHE INTERNAL "")


SET(LOCAL_HEADERS)
FOREACH(INC ${mesh_HEADERS})
  STRING(REPLACE "mesh/" "" LOCAL_INC ${INC})
  SET(LOCAL_HEADERS ${LOCAL_HEADERS} ${LOCAL_INC})
ENDFOREACH()

INSTALL(
  FILES ${LOCAL_HEADERS}
  DESTINATION "${${PROJECT_NAME}_INSTALL_INCLUDE_DIR}/lifev/fsi/mesh"
  COMPONENT ${PACKAGE_NAME}
)
