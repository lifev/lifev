SET(solver_HEADERS
  solver/LevelSetSolver.hpp
  solver/LevelSetData.hpp
CACHE INTERNAL "")

SET(solver_SOURCES
  solver/LevelSetData.cpp
CACHE INTERNAL "")


SET(LOCAL_HEADERS)
FOREACH(INC ${solver_HEADERS})
  STRING(REPLACE "solver/" "" LOCAL_INC ${INC})
  SET(LOCAL_HEADERS ${LOCAL_HEADERS} ${LOCAL_INC})
ENDFOREACH()

INSTALL(
  FILES ${LOCAL_HEADERS}
  DESTINATION "${${PROJECT_NAME}_INSTALL_INCLUDE_DIR}/lifev/level_set/solver"
  COMPONENT ${PACKAGE_NAME}
)
