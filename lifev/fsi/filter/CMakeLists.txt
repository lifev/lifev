SET(filter_HEADERS
  filter/DOFInterfaceIO.hpp
CACHE INTERNAL "")

SET(filter_SOURCES
  filter/DOFInterfaceIO.cpp
CACHE INTERNAL "")


SET(LOCAL_HEADERS)
FOREACH(INC ${filter_HEADERS})
  STRING(REPLACE "filter/" "" LOCAL_INC ${INC})
  SET(LOCAL_HEADERS ${LOCAL_HEADERS} ${LOCAL_INC})
ENDFOREACH()

INSTALL(
  FILES ${LOCAL_HEADERS}
  DESTINATION "${${PROJECT_NAME}_INSTALL_INCLUDE_DIR}/lifev/fsi/filter"
  COMPONENT ${PACKAGE_NAME}
)
